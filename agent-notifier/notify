#!/usr/bin/env bash
set -uo pipefail

if (( $# != 0 )); then
  echo "Usage: notify" >&2
  exit 2
fi

script_path="$(readlink -f "${BASH_SOURCE[0]}")" || exit 1
script_dir="$(dirname "$script_path")"
exec "$script_dir/agent-notify.sh" show
